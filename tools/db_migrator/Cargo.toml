[package]
name = "betterclaw-db-migrator"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow = "1.0"
chrono = { version = "0.4", features = ["clock"] }
rusqlite = { version = "0.31", features = ["bundled"] }
serde_json = "1.0"

[dev-dependencies]
tempfile = "3.14"